import { HomepageDirective } from './homepage.directive';

describe('HomepageDirective', () => {
  it('should create an instance', () => {
    const directive = new HomepageDirective();
    expect(directive).toBeTruthy();
  });
});
